package programTask;

public class MethodAppRunner {

    public static void main(String[] args)  {
        Reusable_Methods reusable_methods = new Reusable_Methods();

        reusable_methods.getPropertyFile();
        reusable_methods.launch_browser();
        reusable_methods.getLinksCount();
        reusable_methods.confirmDialogBox();
        reusable_methods.IsChecked();
        reusable_methods.getAllLinks();
        reusable_methods.cancelDialogBox();
        reusable_methods.getListItems();
        reusable_methods.close_Browser();
    }
}
